import type { DefaultActionHandlers, UseAction } from './types';
export declare const ERROR_MESSAGE = "`useAction` must be called from within `StorageBrowser.Provider`";
export declare const useAction: UseAction<DefaultActionHandlers>;

import type { StatusCounts, Task } from './types';
export declare const getStatusCounts: (tasks?: Task[]) => StatusCounts;
export declare const isProcessingTasks: (statusCounts: StatusCounts) => boolean;
export declare const hasCompletedProcessingTasks: (statusCounts: StatusCounts) => boolean;

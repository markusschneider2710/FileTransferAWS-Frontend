import type { LocationState } from '../../../store';
import type { FoldersState } from './types';
export declare const DEFAULT_LIST_OPTIONS: {
    pageSize: number;
    delimiter: string;
    exclude: "FILE";
};
interface UseFoldersInput {
    destination: LocationState;
    setDestination: (destination: LocationState) => void;
}
export declare const useFolders: ({ destination, setDestination, }: UseFoldersInput) => FoldersState;
export {};

import type { AWSTemporaryCredentials } from '../../storage-internal';
import type { GetLocationCredentials, CredentialsLocation } from './types';
interface StoreRegistrySymbol {
    readonly value: symbol;
}
/**
 * @internal
 */
export declare const createStore: (refreshHandler: GetLocationCredentials, size?: number) => StoreRegistrySymbol;
/**
 * @internal
 */
export declare const getValue: (input: {
    storeSymbol: StoreRegistrySymbol;
    location: CredentialsLocation;
    forceRefresh: boolean;
}) => Promise<{
    credentials: AWSTemporaryCredentials;
}>;
export declare const removeStore: (storeSymbol: StoreRegistrySymbol) => void;
export {};

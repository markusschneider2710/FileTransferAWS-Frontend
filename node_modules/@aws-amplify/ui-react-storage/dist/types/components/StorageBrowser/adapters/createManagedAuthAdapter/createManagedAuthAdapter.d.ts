import type { StorageBrowserAuthAdapter, CreateManagedAuthAdapterInput } from '../types';
/**
 * Create configuration including handlers to call S3 Access Grant APIs to list and get
 * credentials for different locations.
 *
 * @param options - Configuration options for the adapter.
 * @returns - An object containing the handlers to call S3 Access Grant APIs and region
 */
export declare const createManagedAuthAdapter: ({ accountId, credentialsProvider, customEndpoint, region, registerAuthListener, }: CreateManagedAuthAdapterInput) => StorageBrowserAuthAdapter;

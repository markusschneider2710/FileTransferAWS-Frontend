import React from 'react';
import type { NavigationProps } from './Navigation';
export interface ActionDestinationProps {
    isNavigable?: boolean;
    items: NavigationProps['items'];
    label?: string;
}
export declare const ActionDestination: ({ isNavigable, items, label, }: ActionDestinationProps) => React.JSX.Element | null;

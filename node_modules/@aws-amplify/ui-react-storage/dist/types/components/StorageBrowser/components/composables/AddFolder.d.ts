import React from 'react';
export interface AddFolderProps {
    onAddFolder?: () => void;
    isDisabled?: boolean;
    label?: string;
}
export declare const AddFolder: ({ onAddFolder, isDisabled, label, }: AddFolderProps) => React.JSX.Element;

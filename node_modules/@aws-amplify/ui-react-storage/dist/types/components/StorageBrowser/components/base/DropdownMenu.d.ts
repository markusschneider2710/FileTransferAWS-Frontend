import React from 'react';
import type { StorageBrowserIconType } from '../elements';
interface MenuItem {
    isDisabled?: boolean;
    isHidden?: boolean;
    icon?: StorageBrowserIconType;
    id: string;
    label?: string;
}
interface MenuItemProps extends Omit<MenuItem, 'isHidden' | 'id'> {
    onClick?: () => void;
}
export interface MenuProps {
    isDisabled?: boolean;
    items: MenuItem[];
    onItemSelect?: (id: string) => void;
}
export declare function MenuItem({ isDisabled, icon, label, onClick, }: MenuItemProps): React.JSX.Element;
export declare function DropdownMenu({ isDisabled, items, onItemSelect, }: MenuProps): React.JSX.Element;
export {};

import React from 'react';
export interface FolderNameFieldProps {
    id?: string;
    isDisabled?: boolean;
    label?: string;
    placeholder?: string;
    onChange?: (value: string) => void;
    onValidate?: (value: string) => void;
    validationMessage?: React.ReactNode;
}
export declare function FolderNameField({ id, isDisabled, label, onChange, onValidate, placeholder, validationMessage, }: FolderNameFieldProps): React.JSX.Element;

import React from 'react';
export interface DropZoneProps {
    children?: React.ReactNode;
    onDropFiles?: (files: File[]) => void;
}
export declare const DropZone: ({ children, onDropFiles, }: DropZoneProps) => React.JSX.Element;

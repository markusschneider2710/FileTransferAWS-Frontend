import React from 'react';
export interface BreadcrumbProps {
    isCurrent?: boolean;
    name?: string;
    onNavigate?: () => void;
}
interface BreadcrumbNavigationProps {
    breadcrumbs: BreadcrumbProps[];
    role?: React.AriaRole;
}
export declare const Breadcrumb: ({ isCurrent, name, onNavigate, }: BreadcrumbProps) => React.JSX.Element;
export declare function BreadcrumbNavigation({ breadcrumbs, role, }: BreadcrumbNavigationProps): React.JSX.Element;
export {};

import React from 'react';
interface Status {
    name: string;
    count: number;
}
export interface StatusDisplayProps {
    statuses: Status[];
    total: number;
}
export declare const StatusDisplay: ({ statuses, total, }: StatusDisplayProps) => React.JSX.Element | null;
export {};

import React from 'react';
import type { SortDirection } from '../types';
export type SortHeaderProps = {
    content: {
        label?: string;
        sortDirection?: SortDirection;
        onSort?: () => void;
    };
};
export declare const SortHeader: ({ content }: SortHeaderProps) => React.JSX.Element;

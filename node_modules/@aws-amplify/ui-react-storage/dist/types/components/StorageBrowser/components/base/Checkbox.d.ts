import React from 'react';
interface CheckboxProps {
    checked?: boolean;
    disabled?: boolean;
    id?: string;
    labelHidden?: boolean;
    labelText?: string;
    onSelect?: () => void;
}
export declare const Checkbox: ({ checked, disabled, id, labelHidden, labelText, onSelect, }: CheckboxProps) => React.JSX.Element;
export {};

import React from 'react';
export interface SearchFieldProps {
    id?: string;
    label?: string;
    clearLabel?: string;
    submitLabel?: string;
    query?: string;
    placeholder?: string;
    onSearch?: () => void;
    onClear?: () => void;
    onQueryChange?: (query: string) => void;
}
export declare const SearchField: ({ id, label, clearLabel, submitLabel, onSearch, onClear, placeholder, query, onQueryChange, }: SearchFieldProps) => React.JSX.Element;

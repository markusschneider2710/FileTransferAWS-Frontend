import React from 'react';
export interface PaginationProps {
    page?: number;
    hasNextPage?: boolean;
    onPaginate?: (page: number) => void;
    highestPageVisited?: number;
}
export declare const Pagination: ({ page, hasNextPage, onPaginate, highestPageVisited, }: PaginationProps) => React.JSX.Element | null;

import React from 'react';
export type CheckboxHeaderProps = {
    content: {
        checked?: boolean;
        label?: string;
        onSelect?: () => void;
        id?: string;
    };
};
export declare const CheckboxHeader: ({ content, }: CheckboxHeaderProps) => React.JSX.Element;

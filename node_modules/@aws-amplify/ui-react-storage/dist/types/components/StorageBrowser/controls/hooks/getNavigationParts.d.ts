import type { LocationData } from '../../actions';
interface GetNavigationPartsInput {
    location: LocationData;
    path: string;
    includeBucketInPrefix?: boolean;
}
export declare const getNavigationParts: ({ location, path, includeBucketInPrefix, }: GetNavigationPartsInput) => string[];
export {};

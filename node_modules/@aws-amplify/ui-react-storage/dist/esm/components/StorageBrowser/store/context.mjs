import React__default from 'react';
import { noop } from '@aws-amplify/ui';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { DEFAULT_STATE } from './constants.mjs';
import useStoreReducer from './useStoreReducer.mjs';

const DEFAULT_VALUE = [DEFAULT_STATE, noop];
const { StoreContext, useStore } = createContextUtilities({
    contextName: 'Store',
    defaultValue: DEFAULT_VALUE,
});
function StoreProvider({ children, ...props }) {
    const value = useStoreReducer(props);
    return (React__default.createElement(StoreContext.Provider, { value: value }, children));
}

export { StoreContext, StoreProvider, useStore };

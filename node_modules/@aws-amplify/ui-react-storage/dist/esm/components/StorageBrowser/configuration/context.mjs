import React__default from 'react';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { useGetActionInputCallback } from './useGetActionInputCallback.mjs';

const ERROR_MESSAGE = '`useGetActionInput` must be called from within a `ConfigurationProvider`.';
const { useGetActionInput, GetActionInputContext } = createContextUtilities({
    contextName: 'GetActionInput',
    errorMessage: ERROR_MESSAGE,
});
function GetActionInputProvider({ accountId, children, customEndpoint, region, }) {
    const value = useGetActionInputCallback({
        accountId,
        customEndpoint,
        region,
    });
    return (React__default.createElement(GetActionInputContext.Provider, { value: value }, children));
}

export { GetActionInputContext, GetActionInputProvider, useGetActionInput };

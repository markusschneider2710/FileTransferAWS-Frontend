import React__default from 'react';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { useCredentialsStore } from './useCredentialsStore.mjs';

const ERROR_MESSAGE = '`useCredentials` must be called from within a `CredentialsProvider`.';
const { useCredentials, CredentialsContext } = createContextUtilities({
    contextName: 'Credentials',
    errorMessage: ERROR_MESSAGE,
});
function CredentialsProvider({ children, ...props }) {
    const initialValue = React__default.useContext(CredentialsContext);
    const value = useCredentialsStore({ ...props, initialValue });
    return (React__default.createElement(CredentialsContext.Provider, { value: value }, children));
}

export { CredentialsContext, CredentialsProvider, useCredentials };

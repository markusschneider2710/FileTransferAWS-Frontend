import React__default from 'react';
import { ActionExit } from '../components/composables/ActionExit.mjs';
import { useActionExit } from './hooks/useActionExit.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const ActionExitControl = () => {
    const props = useActionExit();
    const Resolved = useResolvedComposable(ActionExit, 'ActionExit');
    return React__default.createElement(Resolved, { ...props });
};

export { ActionExitControl };

import React__default from 'react';
import { AddFolder } from '../components/composables/AddFolder.mjs';
import { useAddFolder } from './hooks/useAddFolder.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const AddFolderControl = () => {
    const props = useAddFolder();
    const Resolved = useResolvedComposable(AddFolder, 'AddFolder');
    return React__default.createElement(Resolved, { ...props });
};

export { AddFolderControl };

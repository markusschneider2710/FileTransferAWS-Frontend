import React__default from 'react';
import { Message } from '../components/composables/Message.mjs';
import { useMessage } from './hooks/useMessage.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const MessageControl = () => {
    const props = useMessage();
    const Resolved = useResolvedComposable(Message, 'Message');
    return React__default.createElement(Resolved, { ...props });
};

export { MessageControl };

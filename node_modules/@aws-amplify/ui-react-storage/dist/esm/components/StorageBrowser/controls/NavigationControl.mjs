import React__default from 'react';
import { Navigation } from '../components/composables/Navigation.mjs';
import { useNavigation } from './hooks/useNavigation.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const NavigationControl = () => {
    const props = useNavigation();
    const Resolved = useResolvedComposable(Navigation, 'Navigation');
    return React__default.createElement(Resolved, { ...props });
};

export { NavigationControl };

import { useControlsContext } from '../context.mjs';

const useActionExit = () => {
    const { data: { actionExitLabel: label, isActionExitDisabled: isDisabled }, onActionExit: onExit, } = useControlsContext();
    return { label, isDisabled, onExit };
};

export { useActionExit };

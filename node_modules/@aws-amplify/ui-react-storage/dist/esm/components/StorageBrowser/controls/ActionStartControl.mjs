import React__default from 'react';
import { ActionStart } from '../components/composables/ActionStart.mjs';
import { useActionStart } from './hooks/useActionStart.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const ActionStartControl = () => {
    const props = useActionStart();
    const Resolved = useResolvedComposable(ActionStart, 'ActionStart');
    return React__default.createElement(Resolved, { ...props });
};

export { ActionStartControl };

import React__default from 'react';
import { ActionsList } from '../components/composables/ActionsList.mjs';
import { useActionsList } from './hooks/useActionsList.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const ActionsListControl = () => {
    const props = useActionsList();
    const Resolved = useResolvedComposable(ActionsList, 'ActionsList');
    return React__default.createElement(Resolved, { ...props });
};

export { ActionsListControl };

import React__default from 'react';
import { LoadingIndicator } from '../components/composables/LoadingIndicator.mjs';
import { useLoadingIndicator } from './hooks/useLoadingIndicator.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const LoadingIndicatorControl = () => {
    const props = useLoadingIndicator();
    const Resolved = useResolvedComposable(LoadingIndicator, 'LoadingIndicator');
    return React__default.createElement(Resolved, { ...props });
};

export { LoadingIndicatorControl };

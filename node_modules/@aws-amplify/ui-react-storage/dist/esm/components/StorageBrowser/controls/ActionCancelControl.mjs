import React__default from 'react';
import { ActionCancel } from '../components/composables/ActionCancel.mjs';
import { useActionCancel } from './hooks/useActionCancel.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const ActionCancelControl = () => {
    const props = useActionCancel();
    const Resolved = useResolvedComposable(ActionCancel, 'ActionCancel');
    return React__default.createElement(Resolved, { ...props });
};

export { ActionCancelControl };

import React__default from 'react';
import { DropZone } from '../components/composables/DropZone.mjs';
import { useDropZone } from './hooks/useDropZone.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const DropZoneControl = ({ children, }) => {
    const props = useDropZone();
    const Resolved = useResolvedComposable(DropZone, 'DropZone');
    return React__default.createElement(Resolved, { ...props }, children);
};

export { DropZoneControl };

import React__default from 'react';
import { ActionDestination } from '../components/composables/ActionDestination.mjs';
import { useActionDestination } from './hooks/useActionDestination.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const ActionDestinationControl = () => {
    const props = useActionDestination();
    const Resolved = useResolvedComposable(ActionDestination, 'ActionDestination');
    return React__default.createElement(Resolved, { ...props });
};

export { ActionDestinationControl };

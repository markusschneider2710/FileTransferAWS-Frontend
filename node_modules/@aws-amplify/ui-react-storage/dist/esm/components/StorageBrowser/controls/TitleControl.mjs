import React__default from 'react';
import { Title } from '../components/composables/Title.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';
import { useTitle } from './hooks/useTitle.mjs';

const TitleControl = () => {
    const props = useTitle();
    const Resolved = useResolvedComposable(Title, 'Title');
    return React__default.createElement(Resolved, { ...props });
};

export { TitleControl };

import React__default from 'react';
import { StatusDisplay } from '../components/composables/StatusDisplay.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';
import { useStatusDisplay } from './hooks/useStatusDisplay.mjs';

const StatusDisplayControl = () => {
    const props = useStatusDisplay();
    const Resolved = useResolvedComposable(StatusDisplay, 'StatusDisplay');
    return React__default.createElement(Resolved, { ...props });
};

export { StatusDisplayControl };

import React__default from 'react';
import { OverwriteToggle } from '../components/composables/OverwriteToggle.mjs';
import { useOverwriteToggle } from './hooks/useOverwriteToggle.mjs';
import { useResolvedComposable } from './hooks/useResolvedComposable.mjs';

const OverwriteToggleControl = () => {
    const props = useOverwriteToggle();
    const Resolved = useResolvedComposable(OverwriteToggle, 'OverwriteToggle');
    return React__default.createElement(Resolved, { ...props });
};

export { OverwriteToggleControl };

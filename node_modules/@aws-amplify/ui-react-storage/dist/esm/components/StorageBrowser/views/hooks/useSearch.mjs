import React__default from 'react';

function useSearch(props) {
    const { onSearch } = props;
    const [searchQuery, setQuery] = React__default.useState('');
    const [isSearchingSubfolders, setIsSearchingSubfolders] = React__default.useState(false);
    const resetSearch = () => {
        setQuery('');
        setIsSearchingSubfolders(false);
    };
    return {
        searchQuery,
        isSearchingSubfolders,
        onSearchQueryChange: (value) => {
            setQuery(value);
        },
        onToggleSearchSubfolders: () => {
            setIsSearchingSubfolders((prev) => !prev);
        },
        onSearchSubmit: () => {
            onSearch?.(searchQuery, isSearchingSubfolders);
        },
        resetSearch,
    };
}

export { useSearch };

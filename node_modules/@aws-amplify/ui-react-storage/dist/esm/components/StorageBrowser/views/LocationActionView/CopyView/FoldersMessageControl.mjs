import React__default from 'react';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { ControlsContextProvider } from '../../../controls/context.mjs';
import { MessageControl } from '../../../controls/MessageControl.mjs';
import '@aws-amplify/ui-react';
import { ViewElement } from '../../../components/elements/definitions.mjs';
import '../../../components/elements/IconElement.mjs';
import '@aws-amplify/ui';
import { STORAGE_BROWSER_BLOCK } from '../../../components/base/constants.mjs';
import '../../../components/composables/context.mjs';
import '@aws-amplify/ui-react-core/elements';
import { useDisplayText } from '../../../displayText/context.mjs';

const defaultValue = {};
const { useFoldersMessage, FoldersMessageProvider } = createContextUtilities({ contextName: 'FoldersMessage', defaultValue });
const FoldersMessageControl = () => {
    const { CopyView: { getListFoldersResultsMessage }, } = useDisplayText();
    const { hasError, folders, message, query, hasExhaustedSearch } = useFoldersMessage();
    const messageContent = getListFoldersResultsMessage({
        hasError,
        folders,
        message,
        query,
        hasExhaustedSearch,
    });
    return (React__default.createElement(ControlsContextProvider, { data: { message: messageContent } },
        React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__message` },
            React__default.createElement(MessageControl, null))));
};
FoldersMessageControl.displayName = 'FoldersMessage';

export { FoldersMessageControl, FoldersMessageProvider, useFoldersMessage };

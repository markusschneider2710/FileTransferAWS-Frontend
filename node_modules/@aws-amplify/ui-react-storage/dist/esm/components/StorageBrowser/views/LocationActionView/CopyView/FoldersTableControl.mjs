import React__default from 'react';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { ControlsContextProvider } from '../../../controls/context.mjs';
import { DataTableControl } from '../../../controls/DataTableControl.mjs';
import { getDestinationPickerTableData } from './getDestinationPickerTableData.mjs';

const defaultValue = {};
const { useFoldersTable, FoldersTableProvider } = createContextUtilities({ contextName: 'FoldersTable', defaultValue });
const FoldersTableControl = () => {
    const { destination, folders, onSelectFolder } = useFoldersTable();
    const { current, path = '' } = destination ?? {};
    const tableData = getDestinationPickerTableData({
        prefix: current?.prefix ?? '',
        path,
        folders,
        onSelectFolder,
    });
    return (React__default.createElement(ControlsContextProvider, { data: { tableData } },
        React__default.createElement(DataTableControl, null)));
};
FoldersTableControl.displayName = 'FoldersTable';

export { FoldersTableControl, FoldersTableProvider, useFoldersTable };

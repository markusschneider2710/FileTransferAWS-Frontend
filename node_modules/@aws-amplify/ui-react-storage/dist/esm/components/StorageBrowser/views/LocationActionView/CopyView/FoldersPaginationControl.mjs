import React__default from 'react';
import { createContextUtilities } from '@aws-amplify/ui-react-core';
import { Pagination } from '../../../components/composables/Pagination.mjs';
import { useResolvedComposable } from '../../../controls/hooks/useResolvedComposable.mjs';

const defaultValue = {};
const { FoldersPaginationProvider, useFoldersPagination } = createContextUtilities({ contextName: 'FoldersPagination', defaultValue });
const FoldersPaginationControl = () => {
    const props = useFoldersPagination();
    const Resolved = useResolvedComposable(Pagination, 'Pagination');
    return React__default.createElement(Resolved, { ...props });
};
FoldersPaginationControl.displayName = 'FoldersPagination';

export { FoldersPaginationControl, FoldersPaginationProvider, useFoldersPagination };

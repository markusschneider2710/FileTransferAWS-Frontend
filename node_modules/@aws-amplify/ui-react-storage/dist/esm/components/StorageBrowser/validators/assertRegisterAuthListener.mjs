import { isFunction } from '@aws-amplify/ui';

function assertRegisterAuthListener(value) {
    if (!isFunction(value)) {
        throw new Error('StorageBrowser: `registerAuthListener` must be a function.');
    }
}

export { assertRegisterAuthListener };

import { createContextUtilities } from '@aws-amplify/ui-react-core';

const defaultValue = {};
const { useComposables, ComposablesProvider } = createContextUtilities({
    contextName: 'Composables',
    defaultValue,
});

export { ComposablesProvider, useComposables };

import React__default from 'react';
import '@aws-amplify/ui-react';
import { ButtonElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import '@aws-amplify/ui';
import { STORAGE_BROWSER_BLOCK } from '../base/constants.mjs';

const ActionExit = ({ onExit, isDisabled = false, label, }) => (React__default.createElement(ButtonElement, { className: `${STORAGE_BROWSER_BLOCK}__exit`, variant: "exit", onClick: onExit, disabled: isDisabled },
    React__default.createElement(IconElement, { className: `${STORAGE_BROWSER_BLOCK}__action-exit-icon`, variant: "exit" }),
    ' ',
    label));

export { ActionExit };

import React__default from 'react';
import '@aws-amplify/ui-react';
import { ButtonElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import '@aws-amplify/ui';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from '../base/constants.mjs';

const DataRefresh = ({ onRefresh, isDisabled = false, }) => (React__default.createElement(ButtonElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__data-refresh`, "aria-label": "Refresh data", variant: "refresh", onClick: onRefresh, disabled: isDisabled },
    React__default.createElement(IconElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__data-refresh-icon`, variant: "refresh" })));

export { DataRefresh };

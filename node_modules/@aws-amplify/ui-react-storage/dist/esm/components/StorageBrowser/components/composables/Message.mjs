import React__default from 'react';
import '@aws-amplify/ui-react';
import { ViewElement, ButtonElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import '@aws-amplify/ui';
import { STORAGE_BROWSER_BLOCK } from '../base/constants.mjs';

const Message = ({ content, id, onDismiss, type, }) => {
    let ariaLabel;
    switch (type) {
        case 'error':
            ariaLabel = 'Error';
            break;
        case 'info':
            ariaLabel = 'Information';
            break;
        case 'warning':
            ariaLabel = 'Warning';
            break;
        case 'success':
            ariaLabel = 'Success';
            break;
    }
    return !content ? null : (React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__message`, role: "alert", variant: type },
        React__default.createElement(IconElement, { variant: type, "aria-label": ariaLabel }),
        React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__message-content` }, content),
        !onDismiss ? null : (React__default.createElement(ButtonElement, { onClick: () => onDismiss(id), className: `${STORAGE_BROWSER_BLOCK}__message-dismiss`, variant: "message-dismiss", "aria-label": "Dismiss message", role: "button" },
            React__default.createElement(IconElement, { variant: "dismiss", className: `${STORAGE_BROWSER_BLOCK}__message-dismiss-icon` })))));
};

export { Message };

import React__default from 'react';
import { defineBaseElement } from '@aws-amplify/ui-react-core/elements';
import { useIcons, STORAGE_BROWSER_ICON_PATHS } from '@aws-amplify/ui-react/internal';
export { STORAGE_BROWSER_ICON_PATHS } from '@aws-amplify/ui-react/internal';

const DEFAULT_ICON_ATTRIBUTES = {
    'aria-hidden': true,
    width: '24',
    height: '24',
    // `viewBox` coordinates map to `path` data in STORAGE_BROWSER_ICON_PATHS
    viewBox: '0 -960 960 960',
    fill: 'none',
    xmlns: 'http://www.w3.org/2000/svg',
    role: 'img',
};
const BaseIconElement = defineBaseElement({
    type: 'svg',
    displayName: 'Icon',
});
const getIconProps = ({ variant, ...props }) => {
    const pathData = variant ? STORAGE_BROWSER_ICON_PATHS[variant] : undefined;
    const children = pathData ? (React__default.createElement("path", { d: pathData, fill: "currentColor" })) : undefined;
    return {
        ...DEFAULT_ICON_ATTRIBUTES,
        ...props,
        children: props.children ?? children,
        variant,
    };
};
const IconElement = (props) => {
    const { variant } = props;
    const icons = useIcons('storageBrowser');
    const icon = variant ? icons?.[variant] : undefined;
    if (icon) {
        return React__default.createElement(React__default.Fragment, null, icon);
    }
    return React__default.createElement(BaseIconElement, { ...getIconProps(props) });
};

export { BaseIconElement, IconElement };

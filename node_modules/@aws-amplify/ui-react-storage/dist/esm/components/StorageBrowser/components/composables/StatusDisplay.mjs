import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK } from '../base/constants.mjs';
import { DescriptionList } from '../base/DescriptionList.mjs';

const StatusDisplay = ({ statuses, total, }) => {
    if (!statuses?.length) {
        return null;
    }
    const descriptions = statuses.map(({ name, count }) => ({
        term: name,
        details: `${count}/${total}`,
    }));
    return (React__default.createElement(DescriptionList, { className: `${STORAGE_BROWSER_BLOCK}__status-display`, descriptions: descriptions }));
};

export { StatusDisplay };

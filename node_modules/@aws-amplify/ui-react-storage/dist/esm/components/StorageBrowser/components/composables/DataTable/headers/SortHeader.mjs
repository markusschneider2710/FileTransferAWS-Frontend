import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import { ButtonElement } from '../../../elements/definitions.mjs';
import { IconElement } from '../../../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK } from '../../../base/constants.mjs';

const SortHeader = ({ content }) => {
    const { label, sortDirection, onSort } = content;
    return (React__default.createElement(ButtonElement, { variant: "sort", className: `${STORAGE_BROWSER_BLOCK}__table-sort-header`, onClick: onSort },
        label,
        React__default.createElement(IconElement, { variant: `sort-${sortDirection ?? 'indeterminate'}` })));
};

export { SortHeader };

import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import { ViewElement, SpanElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from '../base/constants.mjs';

function LoadingIndicator({ label, isLoading, }) {
    return !isLoading ? null : (React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__loading-indicator` },
        React__default.createElement(IconElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__loading-indicator-icon`, variant: "loading" }),
        React__default.createElement(SpanElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__loading-indicator-label`, "aria-live": "polite" }, label)));
}

export { LoadingIndicator };

import React__default from 'react';
import '@aws-amplify/ui-react';
import { ButtonElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from './constants.mjs';

function PaginationButton({ isDisabled, onClick, type, }) {
    if (!type)
        return null;
    const buttonType = `paginate-${type}`;
    return (React__default.createElement(ButtonElement, { "aria-label": `Go to ${type} page`, className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__pagination-button--${buttonType}`, disabled: isDisabled, onClick: onClick, variant: buttonType },
        React__default.createElement(IconElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__pagination-button-icon`, variant: buttonType })));
}

export { PaginationButton };

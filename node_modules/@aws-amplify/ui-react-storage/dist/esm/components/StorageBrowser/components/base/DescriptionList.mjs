import React__default from 'react';
import '@aws-amplify/ui-react';
import { DescriptionListElement, ViewElement, DescriptionTermElement, DescriptionDetailsElement } from '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK } from './constants.mjs';

const Description = ({ term, details }) => {
    return (React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__description` },
        React__default.createElement(DescriptionTermElement, { className: `${STORAGE_BROWSER_BLOCK}__description-term`, role: "term" }, term),
        React__default.createElement(DescriptionDetailsElement, { className: `${STORAGE_BROWSER_BLOCK}__description-details`, role: "definition" }, details)));
};
const DescriptionList = ({ descriptions, className = '', }) => {
    return (React__default.createElement(DescriptionListElement, { className: `${STORAGE_BROWSER_BLOCK}__description-list ${className}`, role: "list" }, descriptions.map(({ term, details }, index) => (React__default.createElement(Description, { term: term, details: details, key: `${index}-description-${term}` })))));
};

export { DescriptionList };

import React__default from 'react';
import { Checkbox } from '@aws-amplify/ui-react';

const CheckboxDataCell = ({ content, }) => {
    const { checked = false, label, onSelect, id } = content;
    return (React__default.createElement(Checkbox, { name: label ?? '', checked: checked, labelHidden: true, label: label, onChange: onSelect, id: id }));
};

export { CheckboxDataCell };

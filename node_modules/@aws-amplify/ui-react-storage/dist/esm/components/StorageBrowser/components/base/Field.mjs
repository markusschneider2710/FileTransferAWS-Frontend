import React__default from 'react';
import '@aws-amplify/ui-react';
import { ViewElement, LabelElement, InputElement } from '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from './constants.mjs';

function Field({ 'aria-describedby': ariaDescribedBy = 'fieldError', className = `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__field`, children, label, icon = null, id, ...props }) {
    return (React__default.createElement(ViewElement, { className: className },
        icon,
        label ? (React__default.createElement(LabelElement, { className: `${className}__label`, htmlFor: id }, label)) : null,
        React__default.createElement(InputElement, { ...props, "aria-describedby": ariaDescribedBy, className: `${className}-input`, id: id, type: 'text' }),
        children));
}

export { Field };

import React__default from 'react';
import { useDropZone } from '@aws-amplify/ui-react-core';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import { ViewElement } from '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK } from '../base/constants.mjs';

const DropZone = ({ children, onDropFiles, }) => {
    const { dragState, ...dropHandlers } = useDropZone({
        onDropComplete: ({ acceptedFiles }) => {
            onDropFiles?.(acceptedFiles);
        },
    });
    return (React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__drop-zone ${STORAGE_BROWSER_BLOCK}__drop-zone${dragState !== 'inactive' ? '--active' : ''}`, ...dropHandlers }, children));
};

export { DropZone };

import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import { ViewElement } from '../../../elements/definitions.mjs';
import '../../../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK } from '../../../base/constants.mjs';

const DateDataCell = ({ content, }) => {
    const { value, displayValue } = content;
    return (React__default.createElement(ViewElement, { className: `${STORAGE_BROWSER_BLOCK}__table-date-data-cell` }, displayValue ?? value?.toLocaleString()));
};

export { DateDataCell };

import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from '../base/constants.mjs';
import { Field } from '../base/Field.mjs';

function FolderNameField({ id, isDisabled, label, onChange, onValidate, placeholder, validationMessage, }) {
    const handleValidate = ({ target: { value }, }) => {
        onValidate?.(value);
    };
    return (React__default.createElement(Field, { "aria-describedby": id, "aria-invalid": !!validationMessage, className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__folder-name-field`, disabled: isDisabled, id: id, label: label, onBlur: handleValidate, onChange: (event) => {
            if (validationMessage)
                handleValidate?.(event);
            onChange?.(event.target.value);
        }, placeholder: placeholder, type: "text" }, validationMessage));
}

export { FolderNameField };

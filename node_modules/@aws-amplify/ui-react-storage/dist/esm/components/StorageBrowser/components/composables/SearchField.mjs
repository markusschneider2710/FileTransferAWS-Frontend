import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import { ButtonElement } from '../elements/definitions.mjs';
import { IconElement } from '../elements/IconElement.mjs';
import { STORAGE_BROWSER_BLOCK_TO_BE_UPDATED } from '../base/constants.mjs';
import { Field } from '../base/Field.mjs';

const SearchField = ({ id, label, clearLabel, submitLabel, onSearch, onClear, placeholder, query = '', onQueryChange, }) => {
    // FIXME: focus not returning to input field after clear
    return (React__default.createElement(React__default.Fragment, null,
        React__default.createElement(Field, { id: id, label: label, icon: React__default.createElement(IconElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__search-field-icon`, variant: "search" }), className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__search-field`, variant: "search", onChange: (e) => {
                onQueryChange?.(e.target.value);
            }, placeholder: placeholder, onKeyUp: (event) => {
                if (event.key === 'Enter') {
                    onSearch?.();
                }
            }, value: query }, query ? (React__default.createElement(ButtonElement, { "aria-label": clearLabel, className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__search-field-clear`, onClick: onClear, variant: "refresh" },
            React__default.createElement(IconElement, { variant: "dismiss" }))) : null),
        React__default.createElement(ButtonElement, { className: `${STORAGE_BROWSER_BLOCK_TO_BE_UPDATED}__search-submit`, onClick: onSearch }, submitLabel)));
};

export { SearchField };

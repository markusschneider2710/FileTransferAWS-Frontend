import React__default from 'react';
import '@aws-amplify/ui';
import '@aws-amplify/ui-react';
import '../elements/definitions.mjs';
import '../elements/IconElement.mjs';
import { DropdownMenu } from '../base/DropdownMenu.mjs';

const ActionsList = ({ isDisabled, items, onActionSelect, }) => {
    return (React__default.createElement(DropdownMenu, { isDisabled: isDisabled, items: items.map((item) => ({ ...item, id: item.actionType })), onItemSelect: onActionSelect }));
};

export { ActionsList };
